{
  "name": "db-maintainer",
  "version": "1.0.0",
  "description": "This code maintains the database for Fuel Efficient. It will update it with new information from the EPA's API. This includes every model from every make and every year that the EPA has measured. All of this is exported to a MySQL database running on AWS.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anshul Singh",
  "license": "ISC",
  "dependencies": {
    "fast-csv": "^4.1.3",
    "mysql2": "^2.1.0",
    "request": "^2.88.2",
    "unzip": "^0.1.11"
  }
}
